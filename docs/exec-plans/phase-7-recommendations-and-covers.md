# Phase 7：推荐榜单 + 推荐度 + 封面上传 + 首页 8 本限制

## 状态

已完成。

当前已完成：

- 新建小说表单支持预先选择封面、即时预览，并在创建成功后自动上传封面。
- 若书籍创建成功但封面上传失败，前端会保留已创建书籍并提示可重试。

## 任务类型

- 新功能
- UI 调整
- API/数据契约变化
- 数据库迁移
- 测试补强
- Harness 更新

## 背景

当前首页已支持内联搜索与快速分类，并将搜索结果展示在首页主区域。但本轮需要进一步把“发现效率”和“内容呈现”做得更像阅读站，同时保持自有风格：

- 首页搜索结果与“最新入库”最多展示 8 本，避免信息噪声；超过 8 本时提供“查看更多”再跳转完整列表页。
- 将“章节热榜”调整为更稳定、可控的“推荐榜单”，排序依据为书籍的推荐度字段（确定性、可配置）。
- 书籍支持上传封面图，且在未上传封面时也必须稳定展示占位图（不返回 404 导致前端裂开）。
- 当前后台/作者管理页只能在小说创建完成后再上传或替换封面，无法在“新建小说”时一次性完成封面设置，创建路径不完整。

## 目标

1. 首页：
   - 搜索结果最多展示 8 本；超过 8 本才显示“查看更多”按钮，点击后跳转到搜索结果页。
   - “最新入库”最多展示 8 本；超过 8 本才显示“查看更多”按钮。
2. 推荐榜单：
   - 新增书籍字段 `recommendScore`（推荐度），按推荐度从大到小排序展示榜单。
   - 推荐度仅允许管理员编辑。
3. 封面：
   - 支持上传书籍封面，最大 10MB。
   - 支持常见图片格式（JPG/PNG/WebP）。
   - 新建小说表单支持在首次提交前选择可选封面，不要求先创建再进入编辑态。
   - 未上传封面时，封面接口必须返回默认占位图，前端可直接使用 `coverUrl` 渲染。
4. 稳定性：
   - 书籍没有任何章节时，详情页仍可正常渲染并展示明确的空状态（不白屏）。

## 非目标

- 不做推荐算法或个性化推荐，仅做可配置的推荐度排序。
- 不引入第三方图床或对象存储，封面仍保存在本地文件系统（可挂载到 Docker volume）。
- 不复制竞品的封面素材或视觉资产。

## 默认实现假设

- `coverUrl` 使用后端接口路径（例如 `/api/books/{bookId}/cover`），保证永远可用：有封面返回封面文件，无封面返回占位图。
- 推荐榜单使用独立接口，默认分页，首页只取前 8 本。
- 推荐度字段默认 `0`，越大越靠前；同分时使用 `created_at desc` 或 `id desc` 做稳定排序。
- “新建小说时设置封面”默认按“一次表单提交、两步后端调用”实现：
  - 前端创建态允许选择封面并展示本地预览。
  - 提交时先调用 `POST /api/me/books` 或 `POST /api/admin/books` 创建书籍。
  - 若用户选择了封面，再立即调用 `POST /api/books/{bookId}/cover` 上传封面。
  - 若封面上传失败但书籍已创建成功，界面必须明确提示“书已创建、封面未上传”，并允许直接重试，不回滚已创建书籍。
- 除非确认需要原子化的单接口创建体验，本轮不新增 `multipart/form-data` 的创建小说接口。

## 影响范围

- 产品：发现页信息结构增强，新增推荐榜单与封面展示。
- UI：首页、书籍卡片、详情页封面区域、后台/作者管理页编辑项增加；新建小说表单增加封面选择、预览和部分失败提示。
- 前端：API types 与 client、HomePage、BookCard、BookDetailPage、管理页表单增加封面与推荐度能力；创建态补齐封面选择与创建后即时上传流程。
- 后端：books 表增量字段；新增推荐榜单查询；新增封面上传与封面读取（含占位图）；复用现有封面上传接口支撑创建后即时上传。
- API：新增/调整书籍字段；新增推荐榜单接口；新增封面上传与读取接口。
- 数据库：books 表新增 `recommend_score`、`cover_path` 与索引。
- 测试：补充推荐排序、权限校验、封面占位图返回、“创建成功但封面上传失败”的回归测试；前端 build 验证。

## 需要更新的 Harness 文件

- `docs/ui-spec.md`
- `docs/api-contract.md`
- `docs/data-model.md`
- `docs/exec-plans/current.md`
- `docs/harness/status.md`（如该文件记录阶段状态）

## 契约变化

### API

- `BookSummary` / `BookDetail` 增加字段：
  - `recommendScore`：number
  - `coverUrl`：string
- 新增：
  - `GET /api/books/recommendations?page&pageSize`
  - `POST /api/books/{bookId}/cover`（multipart 上传）
  - `GET /api/books/{bookId}/cover`（返回图片；缺失时返回占位图）
- 修改：
  - `PATCH /api/books/{bookId}` 仅允许管理员更新 `recommendScore`；作者仍可更新 title/category/description。

### 数据

- `books.recommend_score INT NOT NULL DEFAULT 0`
- `books.cover_path VARCHAR(255) NULL`

### UI 状态

- 首页：
  - 搜索/分类结果在首页主区域展示，最多 8 本，超过才显示“查看更多”。
  - 最新入库最多 8 本，超过才显示“查看更多”。
  - 侧栏榜单替换为“推荐榜单”。
- 详情页：
  - 无章节时显示空状态卡片或插画占位，而不是空白区域。
  - 封面永远有图（真实封面或占位图）。
- 管理页：
  - 增加封面上传入口（管理员 + 作者可用）。
  - 仅管理员页面展示推荐度输入框。
  - 新建小说态允许预先选择封面并展示本地预览。
  - 创建成功后若封面也上传成功，应直接进入带封面的已选中书籍详情。
  - 创建成功后若封面上传失败，应保留已创建书籍，展示明确错误，并允许在当前书籍上重新上传封面。

## 验收标准

- 首页搜索结果和最新入库最多各展示 8 本，且“查看更多”只在超过 8 本时出现。
- 推荐榜单按 `recommendScore desc` 排序，接口与前端一致，且同分排序稳定。
- 仅管理员可以编辑推荐度；管理员与作者都可以上传封面；非作者普通用户不可编辑他人书籍。
- 管理员与作者都可以在“新建小说”时直接选择封面；创建成功后列表和详情立即显示新封面。
- 若创建书籍成功但封面上传失败，用户能收到部分成功提示，书籍不会丢失，并且可在当前编辑页重试上传。
- 未上传封面时封面接口返回占位图（HTTP 200），前端稳定渲染。
- 书籍无章节时，详情页与阅读路径不会白屏或崩溃，页面提示清晰。

## 验证命令

- `make test`
- `cd frontend && npm run build`
- （可选）`docker compose up -d --build` 后浏览器走一遍：首页搜索/快速分类内联结果、推荐榜单、封面上传与展示、无章节书籍详情页。

## 待确认问题

本草案按用户确认结果执行：

- 推荐度权限：仅管理员；封面编辑权限：管理员 + 书籍作者
- 封面最大尺寸：10MB
- 无封面：返回默认占位图
- 新建时封面默认采用“一次提交、创建后立即上传”的两步实现；如需改成单接口原子创建，需要单独调整创建接口为 `multipart/form-data`
